const processingService = {
    async processInput(input) {
      // Implement your processing logic here
      // For now, let's just return the input as an uppercase string
      return input.toUpperCase();
    }
  };
  
  module.exports = processingService;
  